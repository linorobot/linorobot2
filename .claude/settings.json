{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "g=/home/jetson1/.local/bin/graphify; [ -x \"$g\" ] || g=$(command -v graphify) || exit 0; exec \"$g\" hook-guard search"
          }
        ]
      },
      {
        "matcher": "Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "g=/home/jetson1/.local/bin/graphify; [ -x \"$g\" ] || g=$(command -v graphify) || exit 0; exec \"$g\" hook-guard read"
          }
        ]
      }
    ]
  }
}
